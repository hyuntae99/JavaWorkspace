package STUDY_MORE._24_GUI;

public class Main {
    public static void main(String[] args) {

       /*
       JFrame frame = new JFrame(); // 프레임 생성
        frame.setTitle("JFrame title goes here"); // 프레임의 이름

        // 프레임 종료 -> 아래 코드가 없으면 x를 눌러도 프로그램이 종료되지 않음
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);

        frame.setResizable(false); // 프레임 사이즈 조정 불가!
        // frame.setResizable(true); // 프레임 사이즈 조정 가능
        frame.setSize(420, 420); // 프레임의 크기
        frame.setVisible(true); // 프레임 나타내기

        // 아이콘 이미지 설정
        ImageIcon image = new ImageIcon("C:/Java_workspace/JavaWorkspace/src/STUDY_MORE/STUDY_MORE._24_GUI/Logo.png");
        frame.setIconImage(image.getImage()); // 아이콘 변경

        // 배경화면 색깔 변경
        // frame.getContentPane().setBackground(Color.green); // 라이브러리 활용
        // frame.getContentPane().setBackground(new Color(0x123456); // 색상코드 활용
        frame.getContentPane().setBackground(new Color(123,50,250,70)); // RGB+투명도
*/


        // 프레임 파일을 따로 만들어서 관리할 수 있음.
        // MyFrame myFrame = new MyFrame();
        new MyFrame();

    }
}
